name: "Hello World"
description: "Greet someone"
inputs:
  message:
    description: "Message"
    required: true
runs:
  using: "composite"
  steps:
    - run: |
        source src/notification.sh
        send_telegram '${{ inputs.message }}'
      shell: bash